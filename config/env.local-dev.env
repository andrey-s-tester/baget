# Подмешивается после корневого .env в `npm run dev` (см. apps/*/package.json).
# Обнуляет прод-домены: витрина ходит на API через Next (rewrites) и localhost.
NEXT_PUBLIC_API_BASE_URL=
CORS_ORIGINS=
